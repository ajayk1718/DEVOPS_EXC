apiVersion: v1
kind: Pod
metadata:
  name: dynamodb-test
spec:
  serviceAccountName: dynamodb-sa
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command:
      - /bin/sh
      - -c
      - sleep 3600
